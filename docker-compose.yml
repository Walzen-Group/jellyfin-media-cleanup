services:
  jellyfin-media-cleanup:
    build:
      context: .
      args:
        GIT_HASH: "${GIT_HASH:-unknown}"
    ports:
      - "8000:8000"
    env_file:
      - secrets.env
    restart: unless-stopped
